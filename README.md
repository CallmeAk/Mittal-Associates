# Mittal & Associates V3

Add these approved images:
- `assets/hero-law.jpg`: wide legal library, premium office, court architecture, or legal desk image.
- `assets/founder.jpg`: approved profile image.

Replace LinkedIn `#` links with the final URL. Verify the AN-02 map pin before launch. Forms currently open the visitor's email client; use a backend before production resume uploads.

Test locally with `python -m http.server 3000`. Test the news API with `vercel dev`.