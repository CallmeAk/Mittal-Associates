const decode=s=>(s||'').replace(/<!\[CDATA\[/g,'').replace(/\]\]>/g,'').replace(/&amp;/g,'&').replace(/&quot;/g,'"').replace(/&#39;/g,"'").trim();const strip=s=>decode((s||'').replace(/<[^>]*>/g,' ').replace(/\s+/g,' '));const ext=(tag,s)=>{let m=s.match(new RegExp(`<${tag}[^>]*>([\s\S]*?)<\/${tag}>`,'i'));return m?decode(m[1]):''};export default async function handler(req,res){res.setHeader('Cache-Control','s-maxage=21600, stale-while-revalidate=43200');let query=req.query.query||'Indian law',topic=req.query.topic||'Legal Updates',url=`https://news.google.com/rss/search?q=${encodeURIComponent(query)}&hl=en-IN&gl=IN&ceid=IN:en`;try{let r=await fetch(url,{headers:{'User-Agent':'MittalAssociatesLegalUpdates/1.0'}});if(!r.ok)throw Error(r.status);let xml=await r.text(),blocks=[...xml.matchAll(/<item>([\s\S]*?)<\/item>/gi)].slice(0,12),items=blocks.map((m,i)=>{let s=m[1];return{id:`${Date.now()}-${i}`,title:strip(ext('title',s)),link:strip(ext('link',s)),source:strip(ext('source',s))||'Google News',pubDate:strip(ext('pubDate',s)),topic}});res.status(200).json({items})}catch(e){res.status(200).json({items:[],error:'Unavailable'})}}
